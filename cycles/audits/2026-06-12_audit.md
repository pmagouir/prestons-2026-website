# Pre-Merge Audit — 2026-06-12 Cycle (Gate 2)

**Type:** Four-lens adversarial pre-merge review. Branch `monthly-refresh-2026-06`, commits 3126c4f→c29ebdd (HEAD `c29ebdd`).
**Scope:** every changed surface this cycle — `global.css`, `Navigation.astro`, `Footer.astro`, `Hero.astro`, `SkillsToolkit.astro`, `ProjectGrid.astro`, `Layout.astro`, `about.astro`, `consulting.astro`, `experience.astro`, `projects.astro`, `index.astro`, new `personal.astro`, retired `fitness.astro`, `astro.config.mjs`, `lighthouserc.json`, `favicon.svg`, `og-image.png`, `gen_og.mjs`, and the changed content files (`cpip.md`, `scholar-matching.md`, `ai-governance-framework.md`, `uva-ldoe-partnership.md`, `five-lessons-leading-change.md`).
**Bar:** Preston's 9.5/10 on every lens — elegance, taste, care; commissioned-grade.
**Re-scored against:** `2026-06-10_baseline_audit.md` (8.5 / 7.5 / 7.8 / 8.6, overall 8.1).

---

## VERDICT: REVISE

**One Critical finding blocks an unconditional merge; it is narrow and fast to fix.** A third "Ward 7&8" reference survives in the writing-collection essay (`five-lessons-leading-change.md:54`) after the cycle's own draft asserted all Ward 7/8 framing was removed. The essay is **unrendered** (no `/writing` route; confirmed absent from `dist/`), so nothing is public today — but it is a live Pattern-17 landmine against an explicit Preston directive, and the cycle claimed it closed. Fix that one line and the cycle is merge-ready.

Two conditions ride the merge:
- **Release condition (not a code defect):** the three media URLs (CNBC, Inside Higher Ed, Early Learning Nation) still measured HTTP 404 at baseline and render as live outbound links on `/projects`. The Site Lead is resolving them in parallel. Merge must not ship until those resolve (replace or remove + canonical update). Treated as resolution-in-progress per the dispatch.
- **Owner-decision carry-over:** the Common App card's "Founding Leader" quasi-title (`common-app-research.md:3`) renders live on `/projects` and is a recruiter-checkable elevation. The Engineer correctly left it out of this cycle's Gate-1 scope; it is logged Major and recommended for the next cycle, not a block.

Everything else is clean. All seven Gate-1 decisions landed and verify. Every quantitative claim on every changed surface traces to canonical or the BRAIN file canonical cites. JSON-LD is valid (6/6), `sameAs` is exactly LinkedIn + ORCID, Person `url` present. Berlin, equity, Ward 7/8 (public), FP&A banned terms, Gale-Shapley, Career-Connected Learning: all absent from rendered surfaces. The design system landed faithfully against a computed contrast ledger; the footer-hover regression (1.61:1) is fixed to 17.4:1.

### Per-lens scores

| Lens | Baseline | This cycle | Δ | Two-sentence justification |
|---|---|---|---|---|
| **Executive Recruiter** | 8.5 | **9.5** | +1.0 | The pilot is now past-tense and the framework present-tense (C3 cleared), the location line lands in the About sidebar inside the 30-second scan, the 2050 Moonshot is registry-backed in DC CAP's org voice, and FP&A / ~6-report span / primary-architect framing give a recruiter exactly the executive scope they screen for. Held off a 10 only by the "Founding Leader" card label (M-rec-2), which a recruiter can falsify against LinkedIn and which renders live. |
| **Consulting Buyer** | 7.5 | **9.4** | +1.9 | The page is transformed: AI governance now front-doors the practice (M8 cleared — the 2026 demand the OG promised), four curated bands replace the wall of cards, every Results list is fragments with numbers-as-anchors (Pattern 7 cleared), and every figure is registry-clean ($10M+ registered, $500K reframed to capacity recovered, the grant-funding clause cut). A hair under 9.5 only because buyer registers are signalled by a single quiet eyebrow per band rather than fully differentiated CTAs — the planned next step, not a defect. |
| **Brand & Voice** | 7.8 | **9.4** | +1.6 | The editorial palette is finally visible (Hunter-Green CTAs at 11.9:1, burgundy perceivable as active-nav underline + one rule per page), the stock favicon and the OG "&" are gone, type is tokenized to one modular scale, and the new prose is em-dash-clean with no Pattern 8/15/16 violations. Held under 9.5 by the one un-neutralized Ward 7&8 line in the essay collection (C1, the block) and the residual 2-em-dash Common App card summary the cycle correctly left untouched. |
| **Performance & Accessibility** | 8.6 | **9.5** | +0.9 | Measured CWV (Site Lead, commit c29ebdd) clears every gate — home perf 0.99 / LCP 1953ms, all four routes ≥0.99 and well under the 2500ms budget; the LHCI gate now enumerates all six routes (the `/projects` blind spot closed); duplicate `<main>` collapsed on all five pages; `:focus-visible` + `scroll-padding-top` + Escape-to-close shipped; footer hover fixed 1.61:1 → 17.4:1. JSON-LD valid 6/6 with the Person `url` added. axe not re-run live this session (Chrome unavailable in the sandbox); resting-state a11y rests on the baseline's axe-clean result + the Designer's computed OKLCH ledger + static analysis (see Method note). |
| **Overall** | **8.1** | **9.45** | **+1.35** | A commissioned-grade result one line short of an unconditional merge. The correctness work is exact, the design system is deployed with discipline, and the tooling hygiene is excellent; the single Critical is a missed third occurrence in an unrendered file, fast to clear. |

---

## Findings — ranked

Severity: **Critical** = hard-rule / directive / claim-trace violation. **Major** = registered-pattern recurrence, credibility-bearing defect, or release-gating condition. **Minor** = craft / consistency / hygiene.

### Critical (1)

**C1. Un-neutralized "Ward 7&8" reference survives in the writing essay — against an explicit Preston directive the cycle claimed it cleared.**
- **Surface:** `src/content/writing/five-lessons-leading-change.md:54` — *"The data infrastructure we built in 2023 made it possible to see the Ward 7&8 problem clearly."*
- **Rule:** `canonical.md § DC CAP Verified Numbers` + CLAUDE.md hard constraint: "Ward 7/8 framing stays OFF the site entirely" (Preston, 2026-06-02). `glossary.md`/`errors.md` Pattern 2-class directive.
- **Evidence of the miss:** the cycle's own draft (`cycles/content_drafts/2026-06-12_five-lessons-essay_v1.md`, "Net effect") asserts *"All Ward 7/8 framing removed (two passages: intro para 4, Lesson 2)."* It edited lines 12 and 30 correctly (both verified clean in the shipped file) but did not catch a **third** occurrence in Lesson 5 (line 54). The draft's completeness claim is therefore false.
- **Live exposure:** **none today.** No `/writing` route exists (`src/pages/` has no `writing.astro`; `dist/` ships exactly 6 indexed routes; grep for the essay title returns zero `dist/` hits). This is the **Pattern 17 landmine**: the moment a `/writing` route is added, this publishes a banned ward-level frame without a fresh content review.
- **Why Critical not Major:** it violates a bright-line Preston directive in a loaded collection, and the cycle reported the fix as complete when it was not. Verification gates exist to catch exactly the "we already fixed it" miss.
- **Fix:** rephrase line 54 to remove the ward-level frame, swapping in no number (the standing directive: rephrase, do not substitute the corrected 64%). E.g., "made it possible to see the persistence problem in our weakest cohort clearly" or similar — Resume Consultant's wording, consistent with the line-12/line-30 rephrasings already shipped.
- **Owner:** **Resume Consultant** (rewording, Cleanup Mode), Engineer applies.

### Major (4)

**M-1. Three canonical media URLs measured 404 and render as live outbound links on `/projects`. [Release-gating; resolution in progress.]**
- **Surface:** `src/content/media/cnbc-application-spike.md:4`, `inside-higher-ed-minority-applicants.md:4`, `early-learning-nation-child-care.md:4` — these map to clickable cards in `projects.astro:24–36` and render live (verified in `dist/projects/index.html`).
- **Independent finding:** the three files are **unchanged** from the baseline state that measured HTTP 404 (curl, Mozilla UA, follow-redirects). I could not re-measure live this session (no network/curl access in the sandbox); I rely on the baseline measurement + the Site Lead's in-flight note. The URLs in the files are byte-identical to the baseline-flagged dead ones.
- **Status:** the dispatch confirms the Site Lead is resolving these in parallel (live replacements or removal, with a canonical § Media Mentions update). **Per instruction, reported as resolution-in-progress, not a standalone blocker** — but the merge must not ship three dead outbound links on the credibility page. Tie release to their resolution.
- **Owner:** **Performance & SEO** (URL discovery/verification) → Preston approves canonical change → Engineer applies. Auditor re-checks the replacements are live before ship.

**M-2. "Founding Leader" quasi-title renders live on `/projects`. [Recruiter-checkable elevation; correctly deferred, logged for next cycle.]**
- **Surface:** `src/content/projects/common-app-research.md:3` — `context: "The Common Application — Founding Leader, Data Analytics & Research"`. Renders verbatim on the built `/projects` page (verified in `dist/projects/index.html`).
- **Trace:** the canonical verbatim title is **Senior Manager of Research & Analytics** (`canonical.md § Career Trajectory`). Approved framing is "second member of the founding research team" / "founding member of the research function." The card *summary* correctly says "As a founding member of the research function" — only the `context` label carries the elevation. Same failure class as the removed "lead researcher" (MEMORY: role-title accuracy).
- **Why not Critical:** it is a label-register inflation, not a fabricated number, and the body copy is accurate. The Engineer explicitly logged this card as out-of-cycle (diff note, Escalation §3; the experience draft flagged it "for the card if opened"), and Gate 1 did not scope it. So it is a known carry-over, not a regression.
- **Fix (next cycle):** `context: "The Common Application — Research & Analytics (2020–2023)"` and let the accurate founding-member summary carry the signal.
- **Owner:** **Resume Consultant** (next cycle).

**M-3. Em-dash density persists on the one untouched project card (Pattern 6 / baseline M10).**
- **Surface:** `src/content/projects/common-app-research.md:7` (summary): *"…Common App — the largest college application dataset in the United States — I built the analytics environment…"* — 2 prose em-dashes in ~90 words (≈1/45, over the ~1/200 budget). Renders live on `/projects`.
- **Status:** this card was **not touched** this cycle. Every newly written or newly edited surface is em-dash-clean (about, experience, consulting, personal, and the changed CPIP/scholar-matching/AI-gov/UVA cards all count 0 prose em-dashes — verified by source read). The page-level density is down sharply from baseline because the other cards are clean; this single card is the residual.
- **Fix (rides with M-2, same file/next cycle):** restructure the parenthetical pair into separate sentences. ("As a founding member of the research function at Common App, the largest college application dataset in the United States, I built…")
- **Owner:** **Resume Consultant** (next cycle, same pass as M-2).

**M-4. Project-card `context` em-dashes are structural labels, not prose (logged, not a defect this cycle).**
- **Surface:** the `context:` frontmatter on several cards uses an em-dash as a separator ("DC CAP Innovation Hub — Product", etc.). The diff note flags these as structural labels, and the UVA draft noted them acceptable.
- **Auditor position:** concur — these are visual label separators (org — function), not prose, and read as a consistent typographic device across all cards. **Not scored against the prose em-dash budget.** Logged only so a future cycle doesn't mistake a deliberate label convention for a Pattern 6 hit. No action.

### Minor (3)

**m-1. ProjectGrid status label ergonomics — "Live" for two distinct states.** `ProjectGrid.astro:14–18` maps both `operational` projects to "Live"; the Scholar Matching card (in production) and the AI Governance Framework (operational) both read "Live", while CPIP reads "Building" and completed work reads "Completed". This is correct and consistent; noting only that "Live" on the Scholar Matching card sits alongside a 2026-cycle past-tense summary ("That cycle produced…") — the badge and the closing sentence describe different tenses. Cosmetic; no change required this cycle. Owner: Designer (optional, next cycle).

**m-2. `og-image:alt` and OG title use the "and" form correctly, but `og-image.png` is a binary I cannot pixel-verify in this session.** The diff and perf-seo note both confirm the regenerated OG renders "Chief Strategy and Analytics Officer" (m5 fix) and the script change is in `gen_og.mjs:2`. I verified the *referenced* alt text (`Layout.astro:119`) reads "and" and the title metadata is correct, but I did not render the PNG. Owner: Designer/Engineer — confirm the rendered PNG shows "and" before ship (low risk; the script change is in place).

**m-3. Personal-page Gauntlet URL renders live but its registration in canonical is not re-confirmed this session.** `personal.astro:64` links `https://fitness-gauntlet-site.vercel.app/`. The baseline (m3) flagged it unregistered in canonical § Profile URLs; I did not find a registration entry in the canonical I read. It returned 200 at baseline. Low stakes (it is Preston's own tool, on `/personal`, off the positioning real estate), but it should be locked in canonical for completeness. Owner: Resume Consultant (canonical registration, next cycle).

---

## Gate-1 decision verification (all 7 — PASS)

| # | Gate-1 decision | Result | Evidence |
|---|---|---|---|
| a | Career-Connected Learning appears NOWHERE | **PASS** | grep `career.connected` across `src/` → zero hits. Engineer removed the CCL clause from the Innovation paragraph (diff Escalation §1); the experience draft's Edit 3 AFTER copy that named it was overridden by Gate 1. |
| b | Units-led list = the canonical five only | **PASS** | `experience.astro:43` and `about.astro:28` both read "Student Success, University Partnerships, GEAR UP, Innovation, and Data & Technology" — identical, no drift (Pattern 3 clean). |
| c | CTAs are Hunter Green | **PASS** | `Hero.astro:28`, `ProjectGrid.astro:78`, `consulting.astro:25,382` all `bg-primary` + white label + `.on-dark` light focus ring + `hover:bg-primary-hover`. `global.css:9` `--color-primary` = oklch(0.30 0.06 145) = Hunter Green #1F3D2B. White-on-primary = 11.9:1 (computed ledger). |
| d | `/personal` exists, footer-reached; family warm-close stays on About; `/fitness` is a redirect (not old content) | **PASS** | `personal.astro` built (in `dist/`); `Footer.astro:39–44` is the only UI entry to `/personal`; nav has 4 links (no Personal). Family paragraph + 3 photos remain on `about.astro:84–125`. `astro.config.mjs:11–13` emits a 301 `/fitness → /personal`; `dist/fitness/index.html` is a noindex meta-refresh stub (not the old content); `src/pages/fitness.astro` deleted. |
| e | FP&A precision — "leads financial planning and analysis"; banned terms NOWHERE | **PASS** | `experience.astro:35` "I lead the financial planning and analysis function: budgeting, multi-year scenario modeling…". grep `head of finance\|leads finance\|oversees finance\|oversees accounting\|CFO\|controller\|GAAP` across `src/` → zero hits. |
| f | Promotions fact aggregate / no names | **PASS** | `experience.astro:43` "three direct reports have earned promotions, one of them twice" — aggregate, no names; glossary rule 10 (no promotion-ceiling framing) not tripped. |
| g | Speaking block renders "UERU" with no invented expansion | **PASS** | `experience.astro:171` "three conferences: UERU, CIO4Good, and the UVA IES Program Conference" — UERU is the bare acronym (the draft's guessed "Urban Education Researchers Untangled" expansion was correctly NOT shipped — Lesson 7). |

---

## Claim-trace (every number/title/claim/URL on changed surfaces → canonical or cited BRAIN)

**All traced. Specific confirmations requested in the dispatch:**

- **CPIP = 15** — `cpip.md:7` "15 federal data sources"; renders "15" on `/projects` (verified `dist/`). Traces to `canonical.md § Proof-of-Capability #1` (count corrected 19→15, 2026-06-12). No stray "19" anywhere in `src/` (the only "19" on changed surfaces is the AI-governance "19 interactive governance pages", which is correct).
- **AI pilot past-tense + framework present; no pilot-outcome claim** — `experience.astro:47` "I ran the 60-day pilot… from April to June 2026" (past) + "I designed the AI Governance Framework, which now governs how staff use AI day to day" (present). `consulting.astro:63` "I built one… and ran the pilot that put it into practice" (past/present split). `ai-governance-framework.md:7` "I designed… and ran the 60-day pilot" + "it runs in-house" (present). No outcome/results claim anywhere (Lesson 7 honored). Traces to `canonical.md § Proof-of-Capability #3` (status corrected to completed, 2026-06-12).
- **Partner framing (13 in 2026 match / 14 signed)** — `scholar-matching.md:7` "In the 2026 cycle it weighed roughly 700 applicants against 13 partner universities" (cycle frame → 13). `about.astro:28` "14 signed universities" (portfolio frame → 14). Both correct per `canonical.md § Proof-of-Capability #2`. The scrubbed AU board-advisor reference is **absent** (verified — no "board advisor"/AU-advisor text).
- **$10M+ registered** — `consulting.astro:179` "a $10M+ organization's multi-year planning". Now in `canonical.md § DC CAP Verified Numbers` (registered 2026-06-12, `BRAIN/context.md:39`). Clean.
- **2050 Moonshot in DC CAP's voice** — `experience.astro:35` "supporting DC CAP's 2050 Moonshot, the organization's goal of 80% college completion for DC students" — org voice, not Preston's personal claim. Traces to `canonical.md § 2026-06-12 Registrations`. Replaces the prior untraceable occurrence.
- **ORCID in sameAs verified** — `Layout.astro:93` / built `sameAs` carries `https://orcid.org/0000-0003-1093-5312`. Content-verification of record: per `canonical.md § Profile URLs` (locked 2026-06-12) the ORCID public API returned name "Preston Magouirk", UVA PhD affiliation, and the single registered work matching the Louisiana QRIS / AERA Open study. I confirmed the canonical registration and the diff's verification record; I did not independently re-hit the ORCID API this session (no network), but the claim is content-verified in canonical and the URL matches. No Lesson-7 exposure (this is a profile link, not an extrapolated specific).
- **Berlin absent** — grep `berlin\|germany\|ESMT\|relocat` across `src/` + `public/` + `dist/` → zero hits. Location reads "Based in Washington, DC" (`about.astro:142`) and "Washington, D.C." (`consulting.astro:402`). JSON-LD has no address field.
- **Equity absent** — grep `equity\|equit` across `src/` + `dist/` → zero hits.
- **Ward 7/8 absent on all public surfaces** — grep across `src/pages/`, `src/components/`, `src/layouts/`, `dist/` → zero hits. The only occurrence is the unrendered essay (C1).
- **Offline essay neutralized** — two of three landmines cleared (line 12 ward frame + "Over 85%"→"85%"; line 30 ward frame + 38/72 removed), the forbidden "It's not about X; it's about Y" construction removed (line 32 → "The discipline is to ship something that works now…"), MacKenzie spelling fixed (line 14). **One ward frame remains at line 54 (C1).**

**Other changed-surface numbers, all traced:** 85–90% / 75–95% / 67–100% / seven partners 100% / ~700 applicants / 9,100 pairs zero blocking / 140 priority / 70 waitlist / $600K KPMG / $50M+ (instrumental in) / $500K+ recovered / 60%→under 10% / 10→1 staff + 20+→3–5 hrs / four Standards of Practice / two agentic systems / 53-of-53 / ~400,000 pathways / four-tier classification / 4D fluency / 19 governance pages — every one resolves to `canonical.md § DC CAP Verified Numbers` or `§ Operational Discipline Numbers`. CrossFit CF-L3/CCFT + Quarterfinals + top 5–10% Rx + coached-since-2018 (`personal.astro:54`) trace to `canonical.md § Credentials`; the unverified CF-L2/L1/Nutrition/Programming entries (baseline m2) were correctly dropped (folded out per the Engineer diff).

---

## errors.md pattern scan — every changed surface against Patterns 1–16

| # | Pattern | Result on changed surfaces |
|---|---|---|
| 1 | CPIP capability overstate | **CLEAN.** "building" / "launching October 2026" / "It launches in October 2026" everywhere (experience, about, cpip card). No "running"/"operating". |
| 2 | Equity language slip | **CLEAN.** Zero hits, src + dist. |
| 3 | Stale title in About sidebar | **CLEAN.** "Chief Strategy and Analytics Officer" identical across `about.astro:28/139`, `experience.astro:29`, and JSON-LD `jobTitle`. |
| 4 | UVA/LDOE reduced to QRIS only | **CLEAN.** `experience.astro:93` carries the LDOE partnership + policy/accountability framing; `:97` keeps the QRIS study downstream. `uva-ldoe-partnership.md:7` same. Pattern 4 satisfied on both touch points. |
| 5 | Number-rounding drift | **CLEAN on changed surfaces.** CPIP 15 (not 19), "85%" not "Over 85%" in the essay, ~800/14-signed/13-cycle all canonical. |
| 6 | Em-dash overuse | **One residual (M-3):** `common-app-research.md:7` at 2/≈90w (untouched card). All newly written/edited prose = 0 prose em-dashes. Structural `context:` label dashes logged as non-defect (M-4). |
| 7 | Bullet-point prose | **CLEARED on consulting.** All four "Results I've delivered" lists are fragments with numbers-as-anchors (`consulting.astro:93–110, 168–185, 236–256, 308–325`). Pattern 7 fixed — the lens's biggest single gain. |
| 8 | Forbidden self-description | **CLEAN.** No "passionate/driven/on a mission/believes in" on any changed surface. |
| 9 | Missing JSON-LD Person schema | **CLEAN.** Person schema present + valid in built output; `url` added this cycle (perf-seo §4 gap closed at c29ebdd). |
| 10 | Recruiter-tense breakdown | **CLEAN.** Current role present tense ("I lead", "I am"); past roles past tense ("Negotiated", "Led", "Managed editorial operations"). Pilot correctly past (C3 cleared). |
| 11 | Selectivity under-citing | **CLEAN.** "IES Fellow" named; "founding member of the research function at Common App, the largest college application dataset" pairing intact (`experience.astro:66`, `consulting.astro:171`); Peabody Honors Scholar in sidebar. |
| 12 | Credential-led self-description | **CLEAN.** No PhD-led opening on hero, About, or consulting hero. |
| 13 | Family/fitness placement | **CLEAN / resolved.** Family warm-close stays on About; fuller personal content moved to footer-reached `/personal`; CrossFit is one equal-weight sidebar credential line. |
| 14 | "I" where "we" is right | **CLEAN.** Org outcomes are org-framed ("DC CAP sustains", "Our scholars graduate", "Partner graduation rates run"); solo artifacts are first-person ("I designed", "I built one", "I authored"); "the teams I support" (not "my teams") on About operating principles. The baseline's single Pattern-14 exception ("I designed and run" the pilot) is **fixed** (now "I ran… which now governs"). |
| 15 | Empty balanced-clause maxim | **CLEAN.** Manual scan of all new prose: every candidate carries a concrete mechanism. "Most of the work is relational / Some of the work is technical" are topic sentences with substantive follow-through, not standalone maxims. Consulting "Change sticks when leaders pair clear frameworks with disciplined communication…" names mechanisms + a person-subject (Resume Consultant flagged + cleared; concur). |
| 16 | Phantom negation | **CLEAN.** No "none of it was guaranteed"/"no shortcuts"/"wasn't luck". Preston's protected deflation kept: "Far from competitive in the sport" (`personal.astro:54`), "no outside consultant" (`ai-governance-framework.md:7`, a verified fact), "None of this is revolutionary" (essay close) — all correct voice, not phantom negation. The "what is safe to hand to a model and what is not" phrase (`consulting.astro:63`) is two real categories, not the banned "X, not Y" family — concur with the Resume Consultant's flag-and-clear. |

**Baseline proposed Patterns 17–19** (not yet ratified): **17 (unrendered-content landmine)** — directly implicated by C1; the essay is exactly this failure mode and argues for ratifying 17 this cycle. **18 (time-bound claims need expiry)** — the pilot claim was the trigger; cleared this cycle (past tense). **19 (state-level contrast)** — the footer hover was the trigger; cleared (1.61:1 → 17.4:1, verified in `global.css:17` + `Footer.astro:6`).

---

## Performance & Accessibility — method and results

**Performance (used the Site Lead's measured numbers per the crash-avoidance directive; did NOT run a multi-route Lighthouse sweep):** home perf 0.99 / LCP 1953ms / CLS 0.025; projects 0.99 / 1630ms / 0.000; personal 0.99 / 1554ms / 0.000; consulting 0.99 / 1613ms / 0.000 (commit c29ebdd, `2026-06-12_perfseo_note.md` + Site Lead measurement). All clear the 0.9 perf gate and the 2500ms LCP budget — the baseline's LCP-over-budget findings on `/` and `/projects` (M6) are resolved at the measured numbers. CI (`lighthouserc.json`) is the authoritative gate and now enumerates all six routes explicitly (the baseline `/projects` blind spot, M7, is closed — verified in `lighthouserc.json:6–13`).

**Accessibility — static + ledger-based (axe NOT re-run live; Chrome/Bash unavailable in this sandbox session):** per the crash-avoidance directive, I relied on static a11y analysis + the Designer's computed OKLCH contrast ledger (`2026-06-12_navigation-global_v1.md §3.2`, every pairing computed from hex luminance, not estimated) + the baseline's prior axe-clean result (0 WCAG 2.2 AA violations on all routes, axe-core 4.11.4). Static verification of the changed surfaces:
- **Contrast (1.4.3 / 1.4.11):** body text uses neutral-600 (7.2:1) or darker everywhere; neutral-500 (4.35:1, below the 4.5:1 body floor) is not used for body on any changed surface. Green CTAs white-on-#1F3D2B = 11.9:1; hover ground = 9.95:1. **Footer hover fixed:** default neutral-400 7.6:1, hover #F1F5F2 17.4:1 (`Footer.astro:6`, `global.css:17`) — the M4/Pattern-19 regression is gone.
- **Focus (2.4.7 / 2.4.11):** global `*:focus-visible` 2px primary ring + `.on-dark` light ring shipped (`global.css:82–92`); `html { scroll-padding-top: 5rem }` (`global.css:44`) clears the sticky header.
- **Landmarks (landmark-one-main):** duplicate `<main>` collapsed — every changed page wrapper is `<div>`/`<article>`; `Layout.astro:132` supplies the single `<main id="main-content">`. Built `dist/index.html` confirms one `<main>`.
- **Name/Role/Value + keyboard (4.1.2 / 2.1.1):** mobile menu button has `aria-label` + `aria-expanded` + `aria-controls`; Escape-to-close + focus-return shipped (`Navigation.astro:133–138`, confirmed in built JS). Decorative glyphs (`→ ↗ ✓`) carry `aria-hidden="true"` (about, consulting, personal, projects, index, ProjectGrid). Skip-link present (`Layout.astro:128`). `<html lang="en">`.
- **Images (1.1.1):** every `<Image>` on about/personal/hero has an `alt`. Hover-scale removed from non-interactive images and chips (m10): no `hover:scale` on family photos, hero portrait, or skill chips; the Gauntlet tile (a link) correctly keeps its hover.

**Open verification item (carry to the next human-in-the-loop session):** a live axe-core run + manual keyboard/VoiceOver walkthrough on the new `/personal` route and the restructured `/consulting` page. The static + ledger evidence is strong (and the patterns are identical to the baseline's axe-clean surfaces), so I do not gate the verdict on it — but it is the one box the dispatch's tooling constraint left unchecked, and it should run before or right after merge. This is recorded, not waved away.

**JSON-LD validation:** built `dist/index.html` parsed — one Person block, valid. `@type` Person; `url: "https://prestonmagouirk.com"` present (the perf-seo §4 gap, closed); `jobTitle` "Chief Strategy and Analytics Officer"; `worksFor` DC College Access Program; `alumniOf` 3; `hasCredential` 3; `knowsAbout` 8; **`sameAs` = exactly `["https://www.linkedin.com/in/preston-magouirk-840aa757/", "https://orcid.org/0000-0003-1093-5312"]`** — LinkedIn + ORCID only, no Scholar, no GitHub. Matches `canonical.md § Profile URLs` exactly. Perf-SEO reports 6/6 blocks parse clean across `dist/`.

---

## Design-craft / Brand lens — commissioned-grade judgment (Preston's explicit ask)

**The site now reads commissioned-grade.** The single largest move — Hunter-Green CTAs replacing near-black — landed on every primary button and is the change that flips the site from default-Tailwind to editorial. Burgundy is finally perceivable and disciplined: it appears as the active-nav underline (`Navigation.astro:38`, burgundy 10.3:1 on white) and as exactly one section rule per page (consulting "How We Work Together" `border-secondary/30`, SkillsToolkit `border-secondary/30`), honoring the spec's 6:1 green:burgundy ratio rather than decorating. The footer-hover token (`#F1F5F2`, 17.4:1) shipped as specified.

- **Typography scale coherence:** the tokenized modular scale (`--text-display` → `--text-label`, `global.css:21–30`) is applied uniformly; the hero (`text-display`, 76px max) stays the single largest element, page H1s standardize at `text-h1` (64px max). Line-height and letter-spacing are bound per-token inverse to size (`global.css:62–76`). The m6 label-typeface split is fixed: `.text-label` forces Inter regardless of tag (`global.css:72–76`), so "How I Operate" (`about.astro:40`) and the consulting eyebrows render in Inter consistently.
- **Spacing rhythm:** experience uses a fixed white-space scale (`space-y-20` / `space-y-16` / `space-y-4`); consulting bands use uniform `border-t border-neutral-200 pt-12` separation; `max-w-prose` (65ch) on the about and personal narratives gives a proper reading measure. The density reads unhurried.
- **Hunter-Green CTA treatment:** rest `shadow-sm` → hover `shadow-md` + `bg-primary-hover` (light-from-above), white label holds 9.95:1 on the hover ground. Consistent across Hero, ProjectGrid, and both consulting CTAs.
- **Hover/focus states:** the footer-hover regression is fixed (verified). Hover motion removed from non-interactive elements (skill chips static, family photos static, hero portrait static); interactive elements (Gauntlet tile, CTAs, links, nav) keep honest affordances. The footer-hover 17.4:1 token landed (`global.css:17`).
- **Consulting "curated bands":** the four-band layout (governance-first, quiet buyer-register eyebrow → Lora offer name → one green offer line → intro → 2-col offer/results) reads as a practice, not a menu. This is the strongest single craft improvement on the site.
- **`/personal`:** opens on family-adjacent warmth ("the people I come home to and the way I stay sharp for them"), folds the fitness vertical cleanly, keeps the one interactive Gauntlet tile, closes on a quiet email reach-out. Off the positioning real estate, cared-for, footer-reached. Elegant.

**Elements still reading template-grade — named per file:element:**
- `common-app-research.md:3` (`context` label) — "Founding Leader" reads as résumé-inflation, not commissioned restraint; it is the one copy element on `/projects` that undercuts the otherwise-disciplined card register (M-2).
- `common-app-research.md:7` (summary) — the double-em-dash parenthetical is the one prose passage on `/projects` that reads stylistically looser than its neighbors (M-3).
- `index.astro:11` — the home "Selected Work" section still uses ad-hoc `text-3xl sm:text-4xl` on its H2 and a `from-background to-primary/5` wash rather than the tokenized `text-h2`; the Engineer correctly left index outside the 5 surface specs (Lesson 2, no un-spec'd redesign), so this is a next-cycle tokenization opportunity, not a defect. Noting it because a commissioned-grade bar will eventually want index on the same scale as every other page.

These three are the only elements that fall short of the bar, and all three are either deferred-by-design (M-2/M-3, out of Gate-1 scope) or explicitly out of cycle (index). The pages this cycle actually rebuilt are at the bar.

---

## Cleared since baseline (verified this cycle)

- **C3** (pilot present-tense after window closed) — fixed: pilot past, framework present, no outcome claim. Canonical § Proof-of-Capability #3 updated to "completed".
- **C4** ($10M+ unregistered) — registered in canonical § DC CAP Verified Numbers; line kept.
- **C5** (2050 Moonshot unregistered) — registered; reframed to DC CAP org voice.
- **C2** (untraceable "first statewide longitudinal study") — CUT from `experience.astro` and `uva-ldoe-partnership.md`; reframed to "A longitudinal study… under Louisiana's quality rating system."
- **M4 / Pattern 19** (footer hover 1.61:1) — fixed to 17.4:1.
- **M5** (duplicate `<main>` on five pages) — collapsed; one `<main>` per page.
- **M6** (LCP over budget on / and /projects) — resolved at measured numbers (1953ms / 1630ms vs 2500ms budget).
- **M7** (CI never tested /projects) — fixed; lighthouserc enumerates all six routes.
- **M8** (AI governance absent from consulting) — fixed; governance is now the front-door band.
- **M9 / Pattern 7** (bullet-prose on consulting) — fixed; all Results lists are fragments.
- **M11** ("saving $500K+") — fixed to "recovered $500K+ in annual staff capacity".
- **M12** (stock Astro favicon) — replaced with PM monogram.
- **M13** (editorial palette underexpressed) — fixed; Hunter-Green CTAs + perceivable burgundy.
- **M14** (untraceable "funded by national research grants") — CUT; replaced with verified RCT subjects.
- **m1** (subhead "amazing teams who") — rewritten to the cross-sector arc.
- **m2** (unverified CF credentials) — trimmed; only CF-L3/CCFT kept.
- **m6** (label typeface split) — fixed; `.text-label` forces Inter.
- **m7** (untokenized H1 scale) — fixed; tokenized modular scale.
- **m9** ("DC TAG"→"DCTAG", locale) — fixed on About; meta "DC CAP Scholars"→"DC CAP".
- **m10** (hover motion on non-interactive elements) — fixed; static images/chips.
- **m11** (junior toolkit entries) — fixed; "VS Code + Cursor" and "Stata" dropped.
- **m12** (no `:focus-visible` / `scroll-padding-top`) — fixed; both shipped.
- **m13** (Ph.D./M.P.P. dotting, MPP redundancy) — fixed; PhD/MPP/BA, redundancy removed.
- **m14** (meta "DC CAP Scholars") — fixed.
- **m15** (mobile menu Escape) — fixed; Escape closes + returns focus.
- **m16** (nav target size) — fixed; `py-2` on desktop links.
- **m17** (glyph arrows read aloud) — fixed; `aria-hidden` on decorative glyphs.
- **m18** ("Prior"→"Completed" badge) — fixed.
- **Pattern 14 exception** ("I designed and run" the pilot) — fixed.
- **C1 essay landmines** — 3 of 4 cleared (two ward frames, the "Over 85%" drift, the forbidden construction, MacKenzie spelling); **one ward frame remains (line 54) — see C1 above.**

**Still open from baseline (carried, not regressions):** M1 (no `/writing` route — deferred by design); M2 (Founding Leader — M-2 here, next cycle); M3 (media 404s — M-1 here, in-flight); M10 (Common App card em-dashes — M-3 here, next cycle); m3 (Gauntlet URL unregistered — m-3 here); the index-page tokenization (next cycle).

---

## What the verdict turns on

Merge is blocked on **one line** (C1, `five-lessons-leading-change.md:54`) and gated on **one in-flight resolution** (M-1, the three media 404s). Both are narrow. The moment line 54 is reworded and the media URLs resolve (replace or remove + canonical update), this cycle is merge-ready at 9.45 overall — a commissioned-grade result that clears the baseline's five Criticals and M1–M14 work list, deploys the editorial system with discipline, and keeps every number on every changed surface traceable to canonical.

**Findings to the Site Lead:**
- **C1 → Resume Consultant** (reword line 54; Engineer applies). Blocks merge.
- **M-1 → Performance & SEO** (media URL discovery → Preston approves canonical → Engineer applies; Auditor re-checks live before ship). Gates release.
- **M-2, M-3 → Resume Consultant** (next cycle, same `common-app-research.md` pass).
- **m-1, m-2, m-3 → Designer / Engineer / Resume Consultant** (cosmetic / next cycle).
- **Pattern 17 → ratify this cycle** (the essay is the textbook case; the Auditor maintains errors.md and proposes the entry below).

---

## Proposed errors.md additions (Auditor maintains; Site Lead/Preston sign-off)

The baseline proposed Patterns 17–19. This cycle **cleared the triggers for 18 and 19** and **re-triggered 17**. Recommend ratifying all three, with 17's lesson sharpened by C1:

- **Pattern 17 — Unrendered-content landmine.** Content collections are site copy. Anything in `src/content/` passes the same gates as rendered pages, AND a "fix" to collection content is verified line-by-line across the whole file, not just the passages the draft happened to name. (Caught twice now: baseline Ward 7/8 in the orphaned essay; this cycle, a third Ward 7&8 occurrence the cleanup draft missed while asserting completeness.)
- **Pattern 18 — Time-bound status claims need an expiry check.** Every "currently running / in progress" claim carries a known end date; each cycle compares it to today. (Trigger cleared this cycle: the pilot moved to past tense.)
- **Pattern 19 — State-level contrast.** Hover/focus/active colors get the same contrast check as resting state; axe and Lighthouse test resting state only. (Trigger cleared this cycle: footer hover 1.61:1 → 17.4:1.)

---

*Auditor, 2026-06-13 (cycle dated 2026-06-12). Four lenses run in order; every changed surface scanned against errors.md Patterns 1–16 by name; every claim traced to canonical.md or the cited BRAIN source; JSON-LD validated against the built output; Gate-1 decisions verified by grep + source read + built-output read. Performance used the Site Lead's measured CWV (c29ebdd) per the crash-avoidance directive; axe not re-run live (no Chrome/Bash in session) — resting-state a11y rests on the computed OKLCH ledger + static analysis + the baseline's axe-clean result, with a live axe + manual keyboard pass recorded as the one open verification item. Verdict: REVISE (one Critical, one release-gating in-flight item). Counts: 1 Critical, 4 Major, 3 Minor.*
